# Kennel-Booking
This is a kennel booking web server application using Jetty.

Additional feature: Local time at the kennel is shown on the home page

You can execute the jar file in command line by the following command:
1. cd {Path}/Kennel-Booking/target
2. java -jar Kennel-Booking-1.0-SNAPSHOT.jar
